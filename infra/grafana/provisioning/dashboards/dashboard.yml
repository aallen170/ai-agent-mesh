# Grafana provisioned dashboard configuration
apiVersion: 1

providers:
  - name: AIMESH
    orgId: 1
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/dashboards
